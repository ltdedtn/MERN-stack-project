const DashFooter = () => {
  const content = (
    <footer className="footer-center sticky top-[100vh]">
      <p>Current User:</p>
      <p>Status:</p>
    </footer>
  );
  return content;
};
export default DashFooter;
